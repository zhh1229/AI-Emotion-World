# AI Emotion World 项目规则

本文件适用于整个仓库。它定义长期有效的开发约束；具体阶段任务、临时状态和操作细节应写入 `README.md`、阶段说明或提交记录，不要继续堆入本文件。

## 1. 项目目标

- 从 0 到 1 完成一个最小型 AI + Unity 3D 情绪交互 Demo。
- 跑通“玩家输入 -> AI 情绪判断 -> 结构化 JSON -> Unity 解析 -> NPC 与场景反馈”的完整链路。
- 让不具备成熟 Unity、C#、AI 和 3D 建模经验的项目参与者亲自经历开发、验证和排错过程。
- 最终形成结构清晰、可运行、可打包、可上传 GitHub、可由他人按文档复现的学生项目样板。

## 2. MVP 范围

MVP 只包含以下能力：

- 一个小型 Unity 3D 场景。
- 一个可由键鼠控制的玩家。
- 一个可接近并交互的 NPC。
- 一套可输入文本、显示回复的对话 UI。
- 一次 AI 请求与一次结构化响应。
- 五种基础情绪：`happy`、`sad`、`angry`、`calm`、`neutral`。
- Unity 根据情绪和强度切换 NPC 状态，并提供简单场景反馈。
- 至少三种可明确观察和验证的不同情绪反馈。

MVP 的优先级始终是“完整跑通”，不是功能数量、画面质量或架构复杂度。

## 3. 当前技术栈

- 引擎：Unity 3D。
- 语言：C#；辅助工具可使用 Python，但第一版不默认引入自建 Python 后端。
- 交互：PC 键盘鼠标；输入方案在创建 Unity 项目时确认并固定。
- AI 通信：简单 HTTP API + JSON 请求/响应。
- 数据结构：AI 返回可解析的结构化对象，至少包含 `emotion`、`intensity`、`reply`。
- 版本控制：Git + GitHub。

当前仓库尚未创建 Unity 项目。项目初始化后，以 `ProjectSettings/ProjectVersion.txt` 中的实际 Unity 版本为准；不得在文档或规则中虚构 Unity 版本。

## 4. Unity 项目结构

采用按职责拆分的轻量结构，并根据实际需要创建目录，不机械制造空目录：

```text
Assets/
├── Scenes/
├── Scripts/
│   ├── AI/
│   ├── Player/
│   ├── NPC/
│   ├── UI/
│   └── Core/
├── Prefabs/
├── Materials/
├── Models/
├── Animations/
├── Audio/
└── Resources/
Packages/
ProjectSettings/
.gitignore
README.md
AGENTS.md
```

结构规则：

- Unity 资产和脚本放在 `Assets/` 下，不把源代码放在仓库根目录。
- 一个脚本只承担清晰的单一职责，避免把 AI 请求、JSON 解析、UI 和 NPC 行为混在一个类中。
- 场景引用优先通过 Inspector 显式连接；只有确有运行时加载需求时才使用 `Resources/`。
- 外部模型、音频、字体等资产必须记录来源与许可证，并遵守其使用条件。
- 不提交 `Library/`、`Temp/`、`Logs/`、`Obj/`、`Build/`、`Builds/`、`UserSettings/` 及 IDE 自动生成文件。

## 5. Git 规则

- 从第一天使用正常 Git 项目管理，提交必须对应一个清晰、可验证的开发阶段。
- 使用 `main` 作为稳定分支；阶段性开发可按需要建立短生命周期分支。
- 提交信息采用简洁的 Conventional Commits 风格，例如 `feat: create Unity project`、`fix: handle invalid AI JSON`。
- 不提交密钥、个人敏感信息、Unity 缓存、构建产物、依赖缓存或大型无关文件。
- 每次提交前检查 `git status`，确认暂存内容只属于当前阶段。
- 未经用户明确确认，不创建远程 GitHub 仓库，不推送，不强制覆盖历史。
- 不使用破坏性 Git 操作；不得擅自丢弃用户已有改动。

## 6. API Key 安全规则

- 禁止把真实 API Key 写入 C#、JSON、场景、Prefab、文档或任何会被 Git 跟踪的文件。
- 禁止提交 `.env`、本地密钥文件、账号凭据或包含密钥的日志。
- 仓库只允许提供不含真实值的配置模板或 README 配置说明。
- 读取顺序优先考虑本机环境变量和 Unity 外部配置；若 Unity 运行环境无法可靠读取环境变量，再采用明确的本地忽略文件方案。
- `.gitignore` 必须覆盖所有本地密钥文件，README 必须说明如何配置且不暴露真实值。
- 对话、报错和提交内容中不得回显完整密钥；发现泄露时立即停止提交并提醒轮换密钥。

## 7. 代码风格

- 代码必须简单、可读、模块化，让初学者能够追踪数据从输入到反馈的完整流向。
- 类名、方法名和变量名表达真实职责，避免缩写和无意义命名。
- 优先使用 Unity 原生能力和标准库，不为小项目引入大型框架或复杂抽象。
- 一个类保持单一核心职责；AI 请求、数据模型、JSON 解析、NPC 表现和 UI 应分离。
- 对外部请求、空数据、超时、非 2xx 响应、非法 JSON 和未知情绪做明确且可理解的错误处理。
- 注释只解释意图、边界和不明显的原因，不用注释复述代码。
- 不在日志中输出 API Key、完整敏感请求或无关个人信息。
- 修改代码时保持既有风格，只改动完成当前阶段所需的最小范围。

## 8. 第一版明确不允许加入的功能

除非用户明确决定修改 MVP，否则第一版禁止主动加入：

- 多人联机、账号系统、服务器架构、数据库。
- 经济系统、复杂任务系统、开放世界、大型地图。
- 复杂 NPC 社会系统、长期记忆、复杂 Agent 系统。
- 训练大模型或自研复杂情绪模型。
- VR/MR、PICO 接入、语音识别、语音合成、复杂面部捕捉。
- 复杂物理系统、复杂粒子效果、商业级美术和大型资源包。
- ECS、DOTS、复杂依赖注入、复杂设计模式、不必要的大型插件。

发现范围扩张时，应明确标注“属于后续版本，现在不要做”，并说明应从 MVP 中删除或推迟的内容。

## 9. 阶段式开发规则

严格按阶段推进，不为赶进度提前实现后续阶段：

1. 阶段 0：项目规划。
2. 阶段 1：Unity 项目创建 + Git。
3. 阶段 2：最小 3D 场景。
4. 阶段 3：玩家移动。
5. 阶段 4：NPC。
6. 阶段 5：玩家与 NPC 交互。
7. 阶段 6：对话 UI。
8. 阶段 7：AI 接入。
9. 阶段 8：AI JSON 情绪输出。
10. 阶段 9：Unity 情绪解析。
11. 阶段 10：NPC 情绪反应。
12. 阶段 11：简单场景反馈。
13. 阶段 12：整体测试。
14. 阶段 13：代码整理。
15. 阶段 14：README。
16. 阶段 15：Git 提交整理。
17. 阶段 16：最终打包。

每个阶段的回复必须包含：

- 当前阶段与阶段目标。
- 为什么需要这一步。
- AI 可以直接完成的文件、命令或检查工作。
- 用户必须在 Unity、GitHub、浏览器或外部工具中完成的操作。
- 按顺序排列且数量受控的具体步骤。
- 用户应观察到的验证结果。
- 下一阶段是什么。

不要一次性生成整个项目。只有当前阶段通过验证后，才进入下一阶段；若验证失败，先读取错误、定位并修复，不要求用户重建项目。

## 10. 每次修改后的验证要求

每次修改完成前至少执行与改动风险相匹配的验证：

- 检查改动文件和 `git status`，确认没有误改、缓存或密钥。
- Unity C# 改动优先通过 Unity Console 的编译结果或项目实际编译流程检查。
- 涉及场景、Prefab、Inspector 引用、Input System、Tag、Layer 或 Animator 时，明确列出 Unity Editor 中的检查项。
- AI/API 改动应分别验证成功响应、错误响应、超时或非法 JSON；条件不足时明确说明尚未验证的部分。
- 情绪链路改动应验证从输入到 JSON 解析、NPC 状态和场景反馈的完整过程。
- 构建相关改动应在目标平台实际运行或打包验证，不能只凭静态代码判断成功。
- 每次汇报必须区分“已经实际验证”“根据代码推断”和“尚未验证”，不得声称未执行的测试已经通过。
- 阶段验收优先使用最终标准中的可观察结果：场景可运行、玩家可移动、NPC 可交互、对话可显示、AI 结果可解析、情绪可驱动 NPC 和场景、密钥无泄露、仓库状态干净。

完成修改后，用简短语言解释核心模块“做了什么、为什么这样做”，但不要用长篇理论替代可运行结果。
